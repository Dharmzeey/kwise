/**
 * ISR revalidation intervals (in seconds).
 * Change values here to affect all pages at once.
 */

export const REVALIDATE_PAGES = 300;      // home, category, offers — 5 min
export const REVALIDATE_PRODUCT = 300;    // individual product pages — 5 min
export const REVALIDATE_SWAP = 600;       // swap calculator — 10 min
export const REVALIDATE_SITEMAP = 600;    // sitemap — 10 min
